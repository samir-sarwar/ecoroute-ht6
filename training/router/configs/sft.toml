model = "Qwen/Qwen3.5-2B"
algorithm = "sft"
thinking = false

[environment]
id = "${FREESOLO_ENVIRONMENT_ID}"

[environment.params]
split = "train"

[train]
epochs = 3
max_examples = 2000
lora_rank = 32
lora_alpha = 64
max_context_tokens = 4096
